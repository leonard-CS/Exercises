package inkball;

import processing.core.PApplet;
import processing.core.PImage;

public class TileCell extends Cell {
    public TileCell(PImage image, int x, int y) {
        super(image, x, y);
    }
}
